name: Neu-Zuordnung (einmalig)

# Läuft NUR auf Knopfdruck, nie automatisch. Setzt alle fragwürdigen
# Zuordnungen zurück und ermittelt sie mit der aktuellen Bewertungslogik neu.
on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  rematch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Fragwürdige Zuordnungen zurücksetzen
        run: node scripts/reset-for-rematch.js

      - name: TMDB-Zuordnung neu ermitteln
        env:
          TMDB_BEARER_TOKEN: ${{ secrets.TMDB_BEARER_TOKEN }}
        run: node scripts/match-tmdb.js

      - name: Datenqualität bereinigen (Duplikate)
        run: node scripts/cleanup-matches.js

      - name: Unsichere Zuordnungen automatisch nachprüfen
        run: node scripts/auto-verify.js

      - name: Nicht auffindbare Filme aus YouTube-Daten übernehmen
        run: node scripts/absorb-unmatched.js

      - name: Cast-Daten nachladen
        env:
          TMDB_BEARER_TOKEN: ${{ secrets.TMDB_BEARER_TOKEN }}
        run: node scripts/fetch-cast.js

      - name: FSK-Einstufung nachladen
        env:
          TMDB_BEARER_TOKEN: ${{ secrets.TMDB_BEARER_TOKEN }}
        run: node scripts/fetch-fsk.js

      - name: Cover-Bilder cachen
        run: node scripts/cache-images.js

      - name: Nicht abspielbare Filme reparieren
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: node scripts/repair-unavailable.js

      - name: Seite bauen (Index, Filmseiten, Sitemap)
        run: node scripts/build-site.js

      - name: Ergebnisse committen
        run: |
          git config user.name "tubefilme-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/ img/ film/ sitemap.xml robots.txt
          git diff --staged --quiet || git commit -m "Neu-Zuordnung: $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
